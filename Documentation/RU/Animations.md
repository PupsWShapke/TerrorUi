# ✨ Анимации и эффекты

**TerrorUi** использует плавные и высокопроизводительные анимации, чтобы интерфейс казался живым и отзывчивым. Все переходы реализованы через `TweenService`, что обеспечивает стабильную работу при 60 FPS.

---

## 🌪️ Визуальные переходы

Библиотека автоматически применяет анимации к следующим действиям:

* **Открытие/Закрытие окна:** Главное меню появляется и исчезает с эффектом плавного изменения прозрачности (Fade) и размера (Scale).
* **Переключение вкладок:** Контент плавно сменяется, создавая ощущение чистого и современного интерфейса.
* **Взаимодействие с элементами:**
* **Кнопки:** Слегка меняют цвет или размер при наведении курсора и клике.
* **Переключатели (Toggles):** Анимированное движение индикатора при смене состояния.
* **Выпадающие списки:** Плавное раскрытие и сворачивание списка опций.



---

## ⚙️ Оптимизация производительности

Анимации разработаны так, чтобы быть «легкими» и не снижать FPS в игре:

* **Native Interpolation:** Используется стандартный движок интерполяции Roblox.
* **Событийная модель:** Анимации запускаются только в момент взаимодействия, не нагружая процессор в режиме ожидания.
* **Асинхронность:** Анимации UI работают в отдельных потоках, поэтому логика вашего основного скрипта не будет приостанавливаться.

---

## 🛠️ Скриптинг и анимации

Большинство эффектов работают автоматически, но вы можете использовать метод `Toggle`, чтобы вызвать анимацию появления или скрытия всего меню.

```lua
-- Вызывает встроенную анимацию появления/скрытия
GUI:Toggle()

```

### Полезный совет:

Если вы планируете полностью удалить (Destroy) интерфейс, рекомендуется сначала скрыть его анимацией, чтобы это выглядело профессионально:

```lua
MainTab:AddButton("Выгрузить чит", function()
    GUI:Notify("Система", "Выгрузка...", 2)
    GUI:Toggle() -- Плавное исчезновение
    task.wait(0.5) -- Ожидание завершения анимации
    GUI:Destroy() -- Полная очистка
end)

```

---

## 🎨 Эффект наведения (Hover)

Каждый интерактивный элемент имеет встроенное состояние наведения. Когда мышь пользователя оказывается над кнопкой или слайдером, элемент подсвечивается акцентным цветом вашей текущей **Темы**. Это дает мгновенный визуальный отклик игроку.

---

[⬅ Назад к навигации](Navigation.md)
