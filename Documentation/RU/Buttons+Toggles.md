# 🔘 Кнопки и Переключатели

Кнопки и переключатели (Toggle) — это основные элементы взаимодействия, позволяющие пользователю выполнять мгновенные действия или управлять постоянными функциями.

---

## 👆 Кнопки: `AddButton`

Кнопки используются для запуска разовых действий, таких как телепортация, открытие двери или выполнение скрипта.

```lua
Tab:AddButton(text, callback)

```

**Параметры:**

* `text` (string) — Название кнопки.
* `callback` (function) — Функция, которая выполняется при нажатии на кнопку.

**Пример:**

```lua
MainTab:AddButton("Убить всех", function()
    print("Команда на уничтожение отправлена!")
    -- Ваша логика здесь
end)

```

---

## 🔄 Переключатели: `AddToggle`

Переключатели используются для функций, которые имеют два состояния: Включено и Выключено (например, Auto Farm или ESP).

```lua
Tab:AddToggle(text, default, callback)

```

**Параметры:**

* `text` (string) — Название переключателя.
* `default` (boolean) — Начальное состояние (`true` — включен, `false` — выключен).
* `callback` (function) — Функция, которая запускается при смене состояния. Передает аргумент `state` (true/false).

**Пример:**

```lua
MainTab:AddToggle("Бесконечные патроны", false, function(state)
    if state then
        print("Включено")
    else
        print("Выключено")
    end
end)

```

---

## 🛠️ Продвинутое управление

Вы можете сохранить переключатель в переменную, чтобы изменять его состояние программно.

### Ручное изменение состояния

```lua
local myToggle = Tab:AddToggle("Auto Clicker", false, function(v) print(v) end)

-- Изменить состояние на включенное через код
myToggle:SetState(true)

```

---

## 💡 Рекомендации

1. **Обратная связь:** Используйте `GUI:Notify` внутри кнопок, чтобы пользователь понимал, что действие успешно выполнено.
2. **Безопасность:** Если ваша функция (callback) содержит тяжелые циклы `while`, всегда проверяйте состояние `state` переключателя внутри цикла, чтобы иметь возможность его остановить.
3. **Визуализация:** При нажатии кнопка подсвечивается акцентным цветом вашей темы (Красный в Default, Синий в Midnight), давая визуальный отклик.

---

[⬅ Назад к навигации](Navigation.md)
