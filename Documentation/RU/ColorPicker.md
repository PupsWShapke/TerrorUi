# 🎨 Выбор цвета (Color Picker)

Элемент **Color Picker** позволяет пользователю динамически изменять цвета для различных функций, таких как ESP, интерфейс или цвет игровых объектов.

---

## 🖌️ Добавление выбора цвета: `AddColorPicker`

Этот метод создает кнопку, при нажатии на которую открывается панель для выбора цвета из палитры.

```lua
Tab:AddColorPicker(text, default, callback)

```

**Параметры:**

* `text` (string) — Название элемента.
* `default` (Color3) — Начальный цвет (например, `Color3.fromRGB(255, 0, 0)`).
* `callback` (function) — Выполняется при каждом изменении цвета. Передает объект `Color3`.

**Пример:**

```lua
SettingsTab:AddColorPicker("Цвет ESP", Color3.fromRGB(255, 255, 255), function(color)
    print("Выбранный цвет: ", color)
    -- Логика изменения цвета ваших визуалов
end)

```

---

## 🔄 Управление через код

Вы можете сохранить выбор цвета в переменную, чтобы программно обновить его значение.

### Установка цвета

```lua
local myPicker = Tab:AddColorPicker("Цвет темы", Color3.new(1, 0, 0), function() end)

-- Изменяем цвет на синий программно
myPicker:SetColor(Color3.fromRGB(0, 120, 255))

```

---

## 💡 Советы по использованию

1. **Производительность:** Как и слайдер, выбор цвета вызывает callback очень часто, пока пользователь водит курсором по палитре. Если ваша функция "тяжелая", это может вызвать микро-фризы.
2. **Темы:** Вы можете использовать Color Picker для создания полностью настраиваемых тем, привязав выбор цвета к элементам интерфейса.
3. **Хранение:** Если вы планируете сохранять настройки (Config System), сохраняйте значения R, G и B по отдельности или в виде HEX-кода.

---

[⬅ Назад к навигации](Navigation.md)
