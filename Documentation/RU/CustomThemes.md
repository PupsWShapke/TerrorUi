# 🎨 Кастомные темы

В **TerrorUi** предусмотрена гибкая система тем, которая позволяет не только переключаться между готовыми пресетами, но и настраивать внешний вид интерфейса под конкретные задачи.

---

## 🛠️ Создание собственного стиля

Если встроенные темы (**Default** и **Midnight**) вам не подходят, вы можете программно изменять цвета элементов. Внутренняя логика библиотеки построена так, что все компоненты ссылаются на центральный контроллер цветов.

### Определение палитры

Обычно кастомная тема состоит из следующих ключевых параметров:

* **MainBackground** — Цвет основного окна.
* **AccentColor** — Цвет активных элементов (кнопок, ползунков, чекбоксов).
* **SectionColor** — Цвет подложки вкладок и секций.
* **TextColor** — Основной цвет текста.

---

## 🌈 Динамическое управление цветом

Вы можете создать вкладку настроек, где пользователь сам выберет "акцентный" цвет вашего чита или скрипта.

```lua
local SettingsTab = GUI:AddTab("Настройки")

SettingsTab:AddColorPicker("Цвет интерфейса", Color3.fromRGB(255, 0, 0), function(newColor)
    -- Пример вызова обновления акцента (зависит от версии API)
    GUI:UpdateAccent(newColor)
    
    GUI:Notify("Стиль", "Цвет акцента успешно изменен", 2)
end)

```

---

## 📋 Сравнение стандартных конфигураций

Если вы создаете свою тему, ориентируйтесь на эти значения контрастности:

| Элемент | Рекомендуемый тип | Почему это важно |
| --- | --- | --- |
| **Фон** | Темный (RGB < 40) | Снижает нагрузку на глаза при долгой игре. |
| **Акцент** | Яркий / Насыщенный | Помогает быстро найти активные функции. |
| **Текст** | Белый или Светло-серый | Обеспечивает читаемость на любом темном фоне. |

---

## 💡 Советы по дизайну

1. **Единообразие:** Используйте один и тот же акцентный цвет для всех интерактивных элементов. Разные цвета для кнопок и слайдеров сделают UI "грязным".
2. **Прозрачность:** Если библиотека поддерживает `BackgroundTransparency`, старайтесь не делать его слишком высоким (не более 0.2), иначе текст станет трудночитаемым на фоне игровых объектов.
3. **Сохранение:** При создании кастомной темы рекомендуется сохранять выбранный пользователем цвет в `JSON` или `Settings File`, чтобы он применялся автоматически при следующем запуске скрипта.

---

[⬅ Назад к навигации](Navigation.md)
