# 🔽 Выпадающие списки (Dropdowns)

Выпадающие списки — это удобный способ предоставить пользователю выбор одного варианта из множества, не загромождая интерфейс лишними кнопками.

---

## 📑 Создание списка: `AddDropdown`

Используйте этот метод, чтобы добавить меню выбора во вкладку.

```lua
Tab:AddDropdown(text, list, callback)

```

**Параметры:**

* `text` (string) — Название выпадающего списка.
* `list` (table) — Массив строк с вариантами выбора (например, `{"Вариант 1", "Вариант 2"}`).
* `callback` (function) — Выполняется при выборе элемента. Передает строку `selected`.

**Пример:**

```lua
MainTab:AddDropdown("Выбор команды", {"Красные", "Синие", "Зеленые"}, function(selected)
    print("Вы выбрали команду: " .. selected)
end)

```

---

## 🔄 Динамическое управление

Вы можете обновлять содержимое списка или принудительно устанавливать значение во время работы скрипта. Это полезно для списков игроков или локаций.

### Обновление элементов

```lua
local playerDropdown = Tab:AddDropdown("Выбрать игрока", {}, function(name)
    print("Цель: " .. name)
end)

-- Обновление списка игроков:
local names = {}
for _, player in pairs(game.Players:GetPlayers()) do
    table.insert(names, player.Name)
end

playerDropdown:Refresh(names) -- Заменяет старые пункты на новые

```

### Установка значения

Вы можете выбрать элемент программно:

```lua
playerDropdown:SetOption("Player1")

```

---

## 💡 Полезные советы

1. **Пустые списки:** Если изначально таблица `list` пуста, dropdown будет отображаться как пустой до вызова метода `:Refresh()`.
2. **Прокрутка:** Если вариантов слишком много, внутри выпадающего меню автоматически появится прокрутка.
3. **Экономия места:** Используйте dropdown вместо кучи кнопок, если у вас более 3-х однотипных действий (например, выбор режима фарма или цвета ESP).

---

## 📋 Полный пример

```lua
local TerrorUi = loadstring(game:HttpGet("https://raw.githubusercontent.com/PupsWShapke/TerrorUi/refs/heads/main/Main.luau"))()
local GUI = TerrorUi.new("Dropdown Demo", "v1.0", "Default")
local Tab = GUI:AddTab("Настройки")

Tab:AddDropdown("Время суток", {"Утро", "День", "Вечер", "Ночь"}, function(choice)
    if choice == "Утро" then
        -- Логика изменения освещения
    end
    GUI:Notify("Освещение", "Установлено время: " .. choice, 3)
end)

```

---

[⬅ Назад к навигации](Navigation.md)
