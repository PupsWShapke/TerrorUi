# 📂 Иерархия

Понимание того, как устроена структура **TerrorUi**, поможет вам правильно организовывать код и управлять элементами управления.

---

## 🏗️ Структура объектов

Библиотека построена по принципу вложенности. Каждый нижестоящий уровень создается на базе вышестоящего.

1. **Window (Окно)** — Главный контейнер.
* **Tab (Вкладка)** — Страница внутри окна.
* **Elements (Элементы)** — Кнопки, слайдеры и т.д.
* **Sections (Секции)** — (Если поддерживаются) Группировка элементов внутри вкладки.





---

## 🧩 Описание уровней

### 1. Window (Основа)

Это корень вашего интерфейса. Он отвечает за:

* Общий заголовок и версию.
* Переключение тем (`Default`/`Midnight`).
* Глобальные уведомления (`Notify`).
* Видимость всего меню.

### 2. Tabs (Вкладки)

Вкладки создаются внутри окна. Каждая новая вкладка автоматически добавляет кнопку в боковую панель навигации.

* *Важно:* Вы не можете добавить кнопку или слайдер напрямую в Окно, только во Вкладку.

### 3. Elements (Компоненты)

Это конечные точки взаимодействия с пользователем. Они всегда "привязаны" к конкретной вкладке.

* **Действие:** Buttons, Keybinds.
* **Значение:** Sliders, TextBoxes, Color Pickers.
* **Состояние:** Toggles, Dropdowns.

---

## 💻 Пример иерархии в коде

```lua
-- Уровень 1: Window
local GUI = TerrorUi.new("Hierarchy Demo", "1.0", "Default")

    -- Уровень 2: Tab (Вкладка "Main")
    local MainTab = GUI:AddTab("Main")

        -- Уровень 3: Elements (Внутри "Main")
        MainTab:AddButton("Action", function() end)
        MainTab:AddToggle("Feature", false, function() end)

    -- Уровень 2: Tab (Вкладка "Settings")
    local SettingsTab = GUI:AddTab("Settings")

        -- Уровень 3: Elements (Внутри "Settings")
        SettingsTab:AddKeybind("Hide Menu", Enum.KeyCode.RightControl, function() end)

```

---

## 🛡️ Правила взаимодействия

* **Уничтожение:** Если вы вызовете `GUI:Destroy()`, будут удалены все вкладки и все элементы внутри них.
* **Порядок:** Элементы во вкладке отображаются сверху вниз в том порядке, в котором они прописаны в коде.
* **Независимость:** Состояние элемента в одной вкладке (например, Toggle) никак не влияет на элементы в другой вкладке, если вы не настроили это вручную.

---

[⬅ Назад к навигации](Navigation.md)
