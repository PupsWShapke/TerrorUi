# 🚀 Установка

Для начала работы с **TerrorUi** вам не нужно скачивать файлы вручную. Библиотека загружается напрямую через `loadstring`.

---

## 📥 Загрузка библиотеки

Скопируйте этот код в начало вашего скрипта. Он загружает последнюю версию библиотеки с GitHub и инициализирует её.

```lua
local TerrorUi = loadstring(game:HttpGet("https://raw.githubusercontent.com/PupsWShapke/TerrorUi/refs/heads/main/Main.luau"))()

```

---

## 🛠️ Базовый шаблон

Вот минимальный код, необходимый для создания работающего меню:

```lua
-- Загрузка
local TerrorUi = loadstring(game:HttpGet("https://raw.githubusercontent.com/PupsWShapke/TerrorUi/refs/heads/main/Main.luau"))()

-- Создание окна (Заголовок, Версия, Тема)
local GUI = TerrorUi.new("TERROR PROJECT", "v1.0", "Midnight")

-- Создание вкладки
local MainTab = GUI:AddTab("Главная")

-- Добавление кнопки
MainTab:AddButton("Проверить работу", function()
    GUI:Notify("Успех", "Скрипт запущен!", 3)
end)

```

---

## 📋 Требования

Для корректной работы библиотеки убедитесь, что ваш исполнитель (executor) поддерживает:

* **HTTP Requests:** `game:HttpGet` для загрузки кода.
* **Loadstring:** для выполнения загруженного кода.
* **CoreGui / PlayerGui access:** для отрисовки интерфейса.

---

## 💡 Советы по началу работы

1. **Размещение:** Всегда загружайте библиотеку один раз в самом верху скрипта.
2. **Переменные:** Сохраняйте результат `TerrorUi.new` в переменную (обычно `GUI` или `Window`), так как она понадобится вам для создания вкладок и уведомлений.
3. **Безопасность:** Рекомендуется использовать `pcall` при загрузке, чтобы избежать ошибок, если GitHub временно недоступен.

---

[⬅ Вернуться к навигации](Navigation.md)
