# 🧠 Внутренняя логика (Internal Logic)

Понимание того, как **TerrorUi** работает «под капотом», поможет вам писать более эффективные и стабильные скрипты. Библиотека построена на принципах объектно-ориентированного программирования (ООП) и событийно-ориентированной архитектуры.

---

## 🏗️ Объектная модель

Когда вы вызываете методы библиотеки, вы создаете иерархию объектов:

1. **Main Controller (Ядро):** Управляет глобальными состояниями (темы, видимость, очередь уведомлений).
2. **Object Containers:** Каждая вкладка — это отдельный `Frame` со своим списком элементов.
3. **Signals (Сигналы):** Элементы используют внутренние события для передачи данных из UI в ваш код через `callback`.

---

## 🔄 Жизненный цикл вызова (Callback)

Когда пользователь взаимодействует с элементом (например, нажимает кнопку), происходит следующее:

* **Перехват ввода:** Библиотека фиксирует нажатие через `InputBegan` или `MouseButton1Click`.
* **Визуальный отклик:** Запускается `TweenService` для анимации нажатия (изменение цвета/размера).
* **Выполнение логики:** Библиотека вызывает вашу анонимную функцию (`function() ... end`).
* **Передача аргументов:** Если это слайдер или переключатель, библиотека сначала вычисляет новое значение и передает его в функцию как первый аргумент.

---

## 🎨 Система тем и констант

Внутри библиотеки есть таблица цветов (Palette). Метод `SetTheme` не просто меняет цвета, он:

1. Обновляет значения в центральной таблице.
2. Проходит циклом по всем созданным объектам.
3. Плавно перекрашивает их свойства `BackgroundColor3`, `TextColor3` и `ImageColor3` в соответствии с новыми данными.

---

## 📡 Управление уведомлениями (Notification Queue)

Уведомления (`Notify`) не просто накладываются друг на друга. В библиотеке реализована система очереди:

* Каждое новое уведомление вычисляет свою позицию на основе количества уже существующих.
* При исчезновении одного уведомления, остальные плавно перемещаются (сдвигаются), чтобы заполнить пустое пространство.

---

## 🛠️ Технические особенности

* **ZIndex Management:** Библиотека автоматически управляет порядком отрисовки (ZIndex), чтобы выпадающие списки и окна выбора цвета всегда отображались поверх других кнопок.
* **Automatic Sizing:** Используются `UIListLayout` и `UISizeConstraint`, чтобы элементы автоматически подстраивались под количество текста и размер окна.
* **Garbage Collection:** При вызове `Destroy()` библиотека отключает все внутренние соединения (`Disconnect`), чтобы предотвратить утечки памяти в сессии игры.

---

[⬅ Назад к навигации](Navigation.md)

**Что изучим дальше?** Могу рассказать подробнее про **События ввода** или помочь составить **финальный шаблон проекта**.
