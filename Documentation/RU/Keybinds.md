# ⌨️ Горячие клавиши (Keybinds)

Горячие клавиши позволяют пользователям выполнять функции или изменять видимость меню нажатием одной кнопки на клавиатуре.

---

## 🎹 Добавление клавиши: `AddKeybind`

Используйте этот метод, чтобы привязать определенное действие к клавише.

```lua
Tab:AddKeybind(text, defaultKey, callback)

```

**Параметры:**

* `text` (string) — Название функции.
* `defaultKey` (Enum.KeyCode) — Клавиша по умолчанию (например, `Enum.KeyCode.F`).
* `callback` (function) — Выполняется при нажатии на привязанную клавишу.

**Пример:**

```lua
MainTab:AddKeybind("Самоуничтожение", Enum.KeyCode.K, function()
    print("Клавиша нажата!")
    GUI:Destroy()
end)

```

---

## 🔄 Смена клавиши пользователем

Когда пользователь нажимает на элемент Keybind в интерфейсе, система переходит в режим ожидания. Следующая нажатая клавиша автоматически станет новым биндом.

### Программное изменение

Вы также можете изменить клавишу через скрипт:

```lua
local myBind = Tab:AddKeybind("Полет", Enum.KeyCode.G, function() end)

-- Позже меняем клавишу на 'H'
myBind:SetKey(Enum.KeyCode.H)

```

---

## 💡 Важные примечания

1. **Глобальная работа:** Горячие клавиши работают всегда, пока запущен скрипт, даже если само меню скрыто.
2. **Клавиша открытия меню:** Обычно пользователи предпочитают `RightControl` или `Insert` для скрытия/показа меню. Вы можете настроить это вручную:
```lua
game:GetService("UserInputService").InputBegan:Connect(function(input, processed)
    if not processed and input.KeyCode == Enum.KeyCode.RightControl then
        GUI:Toggle()
    end
end)

```


3. **Конфликты:** Старайтесь не использовать стандартные игровые клавиши (`W`, `A`, `S`, `D` или `Space`), чтобы не мешать управлению персонажем.

---

## 📋 Полный пример

```lua
local TerrorUi = loadstring(game:HttpGet("https://raw.githubusercontent.com/PupsWShapke/TerrorUi/refs/heads/main/Main.luau"))()
local GUI = TerrorUi.new("Система биндов", "v1.0", "Midnight")
local Tab = GUI:AddTab("Бой")

Tab:AddKeybind("Включить Reach", Enum.KeyCode.T, function()
    GUI:Notify("Бинд", "Функция Reach активирована!", 2)
end)

```

---

[⬅ Назад к навигации](Navigation.md)
