# 📱 Поддержка мобильных устройств

Этот раздел объясняет, как реализовать и оптимизировать **TerrorUi** для мобильных пользователей, чтобы обеспечить удобное взаимодействие на сенсорных экранах.

---

## 🔘 Кнопка переключения: `CreateMobileButton`

Так как у мобильных игроков нет клавиатуры, чтобы нажать клавишу открытия меню (Bind), необходимо предоставить физическую кнопку для управления видимостью интерфейса.

```lua
GUI:CreateMobileButton()

```

### Ключевые особенности:

* **Перетаскивание:** Пользователи могут зажать и переместить кнопку в любое место на экране.
* **Авто-масштабирование:** Размер кнопки оптимизирован для нажатия пальцем.
* **Умная видимость:** Кнопка автоматически скрывается, когда открыто основное меню, и появляется снова, когда меню закрыто.

---

## 🖐️ Сенсорное взаимодействие

**TerrorUi** построена с использованием `InputService` и событий `Touch`, что означает:

* **Слайдеры:** Можно перетаскивать пальцем или нажимать на полосу для установки значения.
* **Прокрутка:** Списки вкладок и контейнеры элементов поддерживают плавную сенсорную прокрутку.
* **Выпадающие списки:** Увеличенные области нажатия для удобного выбора на маленьких экранах.

---

## 📝 Рекомендации для мобильных устройств

При создании интерфейса для мобильных пользователей учитывайте следующие советы:

1. **Короткие заголовки:** Экраны мобильных устройств узкие; длинные названия вкладок могут обрезаться.
2. **Минимум уведомлений:** Не заспамливайте экран через `GUI:Notify()`, так как уведомления занимают много места на смартфонах.
3. **Тестирование позиции:** Убедитесь, что `CreateMobileButton` не перекрывает стандартный джойстик Roblox или кнопку прыжка.

---

## 🛠️ Пример кода

Полный пример инициализации UI с поддержкой мобильных устройств:

```lua
local TerrorUi = loadstring(game:HttpGet("https://raw.githubusercontent.com/PupsWShapke/TerrorUi/refs/heads/main/Main.luau"))()

-- 1. Создаем окно
local GUI = TerrorUi.new("Mobile Hub", "v1.0", "Midnight")

-- 2. Создаем мобильную кнопку (Обязательно для телефонов)
GUI:CreateMobileButton()

-- 3. Настройка вкладок и элементов
local Tab = GUI:AddTab("Мобильный")

Tab:AddButton("Тест нажатия", function()
    GUI:Notify("Взаимодействие", "Нажатие зарегистрировано!", 3)
end)

Tab:AddToggle("Авто-функция", false, function(state)
    print("Состояние: ", state)
end)

-- 4. Приветственное уведомление
GUI:Notify("Запуск", "Мобильное управление готово.", 5)

```

---

[⬅ Назад к навигации](Navigation.md)
