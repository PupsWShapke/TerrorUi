# 🚀 Советы по производительности

Чтобы ваш интерфейс работал быстро, не вызывал задержек (лагов) и не снижал FPS игрока, следуйте этим рекомендациям при разработке скрипта на **TerrorUi**.

---

## 🏎️ Оптимизация функций (Callbacks)

Самая частая причина лагов — тяжелый код внутри часто вызываемых функций.

* **Слайдеры и Color Pickers:** Эти элементы вызывают функцию `callback` при каждом движении мыши.
* *Плохо:* Выполнять поиск по всей папке `workspace` или делать сложные вычисления внутри слайдера.
* *Хорошо:* Обновлять только переменную или использовать `task.spawn` для тяжелых операций.


* **Использование `task.wait()`:** Если ваш переключатель (Toggle) запускает цикл `while`, обязательно добавляйте задержку, чтобы не "повесить" игру.

---

## 📉 Управление объектами

* **Минимизация обновлений:** Не вызывайте метод `:Refresh()` для выпадающих списков (Dropdown) в бесконечном цикле. Обновляйте список только тогда, когда это действительно нужно (например, при открытии вкладки или при входе нового игрока).
* **Удаление неиспользуемого:** Если ваш скрипт больше не нужен, всегда используйте `GUI:Destroy()`. Это очистит память от созданных GUI-объектов и остановит фоновые процессы библиотеки.

---

## 🧵 Асинхронность и потоки

Если ваша функция должна выполнять длительное действие (например, загрузку данных из интернета или ожидание события), используйте `task.spawn`. Это позволит интерфейсу оставаться отзывчивым, пока выполняется фоновая задача.

```lua
MainTab:AddButton("Загрузить данные", function()
    task.spawn(function()
        -- Длительная операция здесь
        local data = game:HttpGet("...")
        GUI:Notify("Готово", "Данные получены", 2)
    end)
end)

```

---

## 📱 Особенности для слабых устройств

* **Количество элементов:** Хотя библиотека оптимизирована, создание сотен кнопок в одной вкладке может замедлить рендеринг на старых телефонах. Старайтесь распределять функции по разным вкладкам.
* **Уведомления:** Большое количество одновременных уведомлений (`Notify`) создает много графических эффектов. Используйте их только для важных событий.

---

## 💡 Золотое правило

**Интерфейс — это только оболочка.** Старайтесь держать основную логику вашего чита отдельно от кода UI. Это сделает ваш код чище, а работу программы — стабильнее.

---

[⬅ Назад к навигации](Navigation.md)
