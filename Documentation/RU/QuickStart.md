# ⚡ Быстрый старт

Этот раздел поможет вам создать полноценный скрипт за считанные минуты. Мы разберем структуру от инициализации до добавления функционала.

---

## 🚀 Пятишаговый план

1. **Подключение:** Загрузите библиотеку через `loadstring`.
2. **Создание окна:** Инициализируйте основную оболочку интерфейса.
3. **Добавление вкладок:** Разделите ваши функции по категориям.
4. **Наполнение элементами:** Добавьте кнопки, переключатели и слайдеры.
5. **Логика:** Привяжите функции к действиям пользователя.

---

## 📝 Пример готового скрипта

Скопируйте этот код, чтобы увидеть все основные элементы в действии:

```lua
local TerrorUi = loadstring(game:HttpGet("https://raw.githubusercontent.com/PupsWShapke/TerrorUi/refs/heads/main/Main.luau"))()

-- 1. Создаем окно
local Window = TerrorUi.new("My Script Hub", "1.0", "Midnight")

-- 2. Создаем кнопку открытия для мобильных устройств
Window:CreateMobileButton()

-- 3. Добавляем вкладку
local MainTab = Window:AddTab("Главная")

-- 4. Добавляем функции
MainTab:AddButton("Кнопка", function()
    print("Нажато!")
end)

MainTab:AddToggle("Авто-фарм", false, function(state)
    print("Состояние чита: ", state)
end)

MainTab:AddSlider("Скорость", 16, 100, 16, function(value)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
end)

-- 5. Отправляем уведомление при запуске
Window:Notify("Готово", "Скрипт успешно запущен!", 5)

```

---

## 💡 Основные советы

* **Названия вкладок:** Старайтесь делать их короткими (например, "Main", "Combat", "Visuals"), чтобы они не обрезались в меню.
* **Темы:** Если вы хотите красный акцент, замените `"Midnight"` на `"Default"`.
* **Мобильный режим:** Всегда добавляйте `CreateMobileButton()`, если планируете использовать скрипт на телефонах или планшетах.

---

[⬅ Назад к навигации](Navigation.md)
