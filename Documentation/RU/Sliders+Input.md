# 🎚️ Слайдеры и Ввод (Sliders & Inputs)

Этот раздел посвящен элементам, которые позволяют пользователю вводить точные числовые значения или текстовые данные.

---

## 📏 Слайдеры: `AddSlider`

Слайдеры позволяют выбирать числовое значение в заданном диапазоне, перетаскивая ползунок.

```lua
Tab:AddSlider(text, min, max, default, callback)

```

**Параметры:**

* `text` (string) — Название слайдера.
* `min` (number) — Минимальное значение.
* `max` (number) — Максимальное значение.
* `default` (number) — Значение по умолчанию при загрузке.
* `callback` (function) — Выполняется при каждом изменении ползунка. Передает текущее `value`.

**Пример:**

```lua
MainTab:AddSlider("Скорость ходьбы", 16, 100, 16, function(value)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
end)

```

---

## ⌨️ Текстовые поля: `AddTextBox`

Текстовые поля позволяют пользователю вводить строки или числа (например, никнейм игрока или координаты).

```lua
Tab:AddTextBox(text, placeholder, callback)

```

**Параметры:**

* `text` (string) — Название или описание поля ввода.
* `placeholder` (string) — Серый текст-подсказка, видимый, когда поле пустое.
* `callback` (function) — Выполняется, когда пользователь нажимает **Enter** или убирает фокус с поля. Передает строку `input`.

**Пример:**

```lua
SettingsTab:AddTextBox("Ник игрока", "Введите имя...", function(text)
    print("Введенный текст: " .. text)
end)

```

---

## 🛠️ Продвинутое использование

### Динамическое обновление слайдера

Вы можете сохранить слайдер в переменную, чтобы изменить его значение через код:

```lua
local mySlider = Tab:AddSlider("FOV", 70, 120, 70, function(v) 
    workspace.CurrentCamera.FieldOfView = v 
end)

-- Сброс FOV до 70 программно
mySlider:SetValue(70)

```

### Валидация ввода

Поскольку `AddTextBox` всегда возвращает строку, при работе с числами стоит использовать `tonumber()`:

```lua
Tab:AddTextBox("Сила прыжка", "1-200", function(input)
    local num = tonumber(input)
    if num then
        game.Players.LocalPlayer.Character.Humanoid.JumpPower = num
    else
        GUI:Notify("Ошибка", "Пожалуйста, введите число!", 3)
    end
end)

```

---

## 💡 Советы

1. **Производительность:** Слайдер вызывает callback очень часто при перетаскивании. Старайтесь не ставить туда "тяжелые" функции.
2. **Очистка:** Текст в поле ввода обычно сохраняется после нажатия Enter, что удобно для повторного ввода.

---

[⬅ Назад к навигации](Navigation.md)
