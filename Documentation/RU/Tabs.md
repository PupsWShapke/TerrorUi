# 📑 Вкладки (Tabs)

Вкладки используются для разделения функций вашего скрипта на логические категории (например, Combat, Visuals, Misc). Каждая созданная вкладка автоматически добавляет кнопку в боковое меню навигации.

---

## 📂 Создание вкладки: `AddTab`

Чтобы добавить новую страницу в ваше меню, используйте метод `AddTab` у объекта окна.

```lua
local Tab = GUI:AddTab(name)

```

**Параметры:**

* `name` (string) — Название вкладки, которое будет отображаться в боковой панели.

**Пример:**

```lua
local MainTab = GUI:AddTab("Главная")
local CombatTab = GUI:AddTab("Бой")

```

---

## 🛠️ Наполнение вкладки

После того как вкладка создана и сохранена в переменную, вы можете добавлять в неё интерактивные элементы.

```lua
-- Добавление элементов во вкладку "Главная"
MainTab:AddButton("Кнопка", function()
    print("Нажато!")
end)

MainTab:AddToggle("Переключатель", false, function(state)
    print("Статус: ", state)
end)

```

---

## 💡 Советы по использованию

1. **Краткость:** Используйте короткие названия для вкладок (1-2 слова), чтобы они корректно отображались в боковом меню и не перекрывали друг друга.
2. **Лимит вкладок:** Хотя библиотека поддерживает создание множества вкладок, старайтесь не добавлять их слишком много, чтобы пользователю не приходилось долго прокручивать список.
3. **Организация:** Группируйте похожие функции. Например, всё, что связано с внешним видом (ESP, Tracers), выносите во вкладку "Visuals".

---

## 📋 Пример структуры

```lua
local TerrorUi = loadstring(game:HttpGet("https://raw.githubusercontent.com/PupsWShapke/TerrorUi/refs/heads/main/Main.luau"))()
local GUI = TerrorUi.new("Multi-Tab Hub", "v1.0", "Midnight")

-- Создаем несколько разделов
local FarmTab = GUI:AddTab("Фарм")
local TeleportTab = GUI:AddTab("Телепорты")
local SettingsTab = GUI:AddTab("Настройки")

-- Добавляем контент в конкретную вкладку
SettingsTab:AddButton("Удалить интерфейс", function()
    GUI:Destroy()
end)

```

---

[⬅ Назад к навигации](Navigation.md)
