# 🎨 Темы

В **TerrorUi** встроена система тем, которая позволяет мгновенно менять внешний вид всего интерфейса. Вы можете выбрать один из готовых пресетов, чтобы адаптировать меню под свои предпочтения.

---

## 🌓 Встроенные темы

Библиотека включает два профессиональных пресета. Установить тему можно как при создании окна, так и динамически во время работы скрипта.

```lua
-- При инициализации
local GUI = TerrorUi.new("Project", "v1.0", "Midnight")

-- Переключение в процессе работы
GUI:SetTheme("Default")

```

| Название | Фон | Акцентный цвет | Стиль |
| --- | --- | --- | --- |
| `"Default"` | Темно-серый | **Красный** | Классический / Агрессивный |
| `"Midnight"` | Глубокий черный | **Синий** | Современный / Sleek |

---

## 🛠️ Переключатель тем в меню

Вы можете добавить выпадающий список в настройки вашего скрипта, чтобы пользователи сами выбирали оформление.

```lua
SettingsTab:AddDropdown("Тема интерфейса", {"Default", "Midnight"}, function(choice)
    GUI:SetTheme(choice)
    GUI:Notify("Тема изменена", "Выбран стиль: " .. choice, 3)
end)

```

---

## 🌈 Изменение акцента

Если ваша версия библиотеки поддерживает метод `UpdateAccent`, вы можете позволить пользователям выбирать любой цвет для активных элементов (кнопок, ползунков, чекбоксов).

```lua
SettingsTab:AddColorPicker("Цвет акцента", Color3.fromRGB(255, 0, 0), function(newColor)
    GUI:UpdateAccent(newColor) 
end)

```

---

## 💡 Как это работает

* **Плавность:** Смена цветов происходит через `TweenService`, что делает переход мягким и приятным для глаз.
* **Глобальность:** При вызове `SetTheme` обновляются все элементы во всех вкладках одновременно.
* **Производительность:** Система оптимизирована так, чтобы частая смена тем не вызывала задержек (лагов) в игре.

---

[⬅ Назад к навигации](Navigation.md)
