# 🏗️ Управление окном (Window Management)

Окно — это фундамент вашего интерфейса. Оно управляет глобальным состоянием, темами и уведомлениями.

---

## 🟢 Создание окна: `TerrorUi.new`

Это первая строка кода, которую необходимо вызвать после загрузки библиотеки. Она инициализирует основной фрейм.

```lua
local GUI = TerrorUi.new(title, version, theme)

```

**Параметры:**

* `title` (string) — Название вашего хаба (отображается в верхнем левом углу).
* `version` (string) — Тег версии (отображается серым цветом рядом с заголовком).
* `theme` (string) — Стартовая тема. Варианты: `"Default"` или `"Midnight"`.

**Пример:**

```lua
local GUI = TerrorUi.new("TERROR PROJECT", "v3.1", "Midnight")

```

---

## 📱 Поддержка мобильных устройств: `CreateMobileButton`

Чтобы сделать ваше меню доступным для мобильных игроков, используйте этот метод. Он создает плавающую кнопку с логотипом "T", которую можно перетаскивать. Она переключает видимость меню.

```lua
GUI:CreateMobileButton()

```

---

## 🔔 Уведомления: `Notify`

Уведомления появляются в виде стильных всплывающих окон в правом нижнем углу экрана. Они идеально подходят для обновлений статуса или приветственных сообщений.

```lua
GUI:Notify(title, content, duration)

```

**Параметры:**

* `title` (string) — Жирный заголовок уведомления.
* `content` (string) — Основной текст сообщения.
* `duration` (number) — Сколько секунд уведомление будет на экране (по умолчанию: 5).

**Пример:**

```lua
GUI:Notify("Обновление", "Новые функции были загружены!", 4)

```

---

## 👁️ Видимость: `Toggle`

Вы можете программно скрыть или показать меню с помощью этого метода. Он включает плавную анимацию появления/исчезновения.

```lua
GUI:Toggle()

```

---

## 🎨 Смена темы: `SetTheme`

Мгновенно изменяет цветовую палитру всего интерфейса.

```lua
GUI:SetTheme(themeName)

```

**Доступные темы:**

* `"Default"` — Темно-серый фон с **красными** акцентами.
* `"Midnight"` — Глубокий черный фон с **синими** акцентами.

---

## 🗑️ Удаление UI: `Destroy`

Если вам нужно полностью удалить интерфейс с экрана игрока и очистить память (например, при выгрузке скрипта):

```lua
GUI:Destroy()

```

---

[⬅ Назад к навигации](Navigation.md)
