# 🔘 Кнопки та перемикачі (Buttons & Toggles)

Це основні інтерактивні елементи для керування функціями вашого скрипта.

---

## 🖱️ Кнопка (Button)

Використовується для виконання одноразової дії при натисканні.

```lua
Tab:AddButton(name, callback)

```

**Параметри:**

* `name` (string) — Текст на кнопці.
* `callback` (function) — Функція, яка виконається при натисканні.

**Приклад:**

```lua
MainTab:AddButton("Знищити UI", function()
    GUI:Destroy()
end)

```

---

## 🔘 Перемикач (Toggle)

Використовується для ввімкнення або вимкнення певної функції. Зберігає свій стан (true/false).

```lua
Tab:AddToggle(name, default, callback)

```

**Параметри:**

* `name` (string) — Назва функції.
* `default` (boolean) — Початковий стан (`true` — увімкнено, `false` — вимкнено).
* `callback` (function) — Функція, що отримує нове значення (`state`) при зміні.

**Приклад:**

```lua
MainTab:AddToggle("Авто-фарм", false, function(state)
    _G.AutoFarm = state
    if state then
        print("Функцію увімкнено")
    else
        print("Функцію вимкнено")
    end
end)

```

---

## ⚙️ Особливості елементів

1. **Візуальний відгук:** Кнопки та перемикачі мають вбудовану анімацію натискання та зміни кольору.
2. **Потоки (Threading):** Кожен `callback` автоматично виконується в окремому потоці через `task.spawn`, тому важкі обчислення не "заморозять" інтерфейс.
3. **Динамічне оновлення:** Перемикачі автоматично змінюють колір індикатора залежно від обраної теми.

---

[⬅ Назад до навігації](Navigation.md)
