# 🎨 Палітра кольорів (Color Picker)

Елемент вибору кольору дозволяє користувачеві налаштовувати візуальні параметри скрипта, такі як колір ESP, інтерфейсу або ефектів.

---

## 🌈 Створення вибору кольору: `AddColorPicker`

```lua
Tab:AddColorPicker(name, default, callback)

```

**Параметри:**

* `name` (string) — Назва налаштування.
* `default` (Color3) — Початковий колір за замовчуванням.
* `callback` (function) — Функція, що отримує обраний колір (`Color3`) як аргумент.

**Приклад:**

```lua
Tab:AddColorPicker("Колір гравців", Color3.fromRGB(255, 0, 0), function(color)
    print("Обрано новий колір: ", color)
end)

```

---

## 🎨 Інтерфейс палітри

При натисканні на елемент відкривається розширене вікно, яке містить:

1. **Поле відтінку:** Для вибору насиченості та яскравості.
2. **Слайдер спектра:** Для вибору основного тону (RGB).
3. **Попередній перегляд:** Відображення поточного обраного кольору в реальному часі.

---

## ⚙️ Особливості роботи

1. **Пряма передача:** Callback повертає стандартний об'єкт `Color3`, який можна відразу використовувати для властивостей об'єктів Roblox.
2. **Авто-закриття:** Вікно вибору кольору автоматично згортається при натисканні поза його межами або при перемиканні вкладок.
3. **Збереження стану:** Вибраний колір відображається на невеликому квадраті поруч із назвою елемента навіть у згорнутому стані.

---

[⬅ Назад до навігації](Navigation.md)
