# 📑 Випадаючі списки (Dropdowns)

Випадаючі списки дозволяють користувачеві обрати один варіант із заздалегідь визначеного переліку. Це ідеальний інструмент для вибору режимів, гравців або предметів.

---

## 📂 Створення списку: `AddDropdown`

```lua
Tab:AddDropdown(name, list, callback)

```

**Параметри:**

* `name` (string) — Назва елемента.
* `list` (table) — Масив рядків (тексту), які будуть доступні для вибору.
* `callback` (function) — Функція, що отримує обраний текст (`option`) як аргумент.

**Приклад:**

```lua
Tab:AddDropdown("Оберіть режим", {"Safe", "Blatant", "Legit"}, function(option)
    print("Обрано режим: " .. option)
end)

```

---

## 🔄 Оновлення списку: `Refresh`

Ви можете динамічно змінювати вміст списку після його створення (наприклад, для оновлення списку гравців на сервері).

```lua
local MyDropdown = Tab:AddDropdown("Гравці", {}, function(val) print(val) end)

-- Оновлення списку новими даними
MyDropdown:Refresh({"Player1", "Player2", "Player3"})

```

---

## ⚙️ Особливості елемента

1. **Автоматична висота:** Список розгортається вниз, адаптуючись до кількості елементів.
2. **ZIndex Management:** Розгорнутий список завжди відображається поверх інших кнопок та слайдерів у вкладці.
3. **Плавність:** Відкриття та закриття супроводжуються анімацією.
4. **Скролінг:** Якщо елементів занадто багато, всередині списку з'являється прокрутка, щоб не виходити за межі вікна.

---

[⬅ Назад до навігації](Navigation.md)
