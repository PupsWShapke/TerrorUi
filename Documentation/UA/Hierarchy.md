# ⚙️ Робочий процес бібліотеки (Workflow)

Розуміння життєвого циклу **TerrorUi** дозволяє будувати стабільні скрипти та уникати конфліктів при виконанні коду.

---

## 🔄 Послідовність виконання

Процес роботи бібліотеки можна розділити на чотири основні етапи:

1. **Ініціалізація (Bootstrapping):**
Завантаження вихідного коду через `loadstring`. На цьому етапі створюється глобальна таблиця функцій та констант.
2. **Побудова інтерфейсу (Structure):**
Створення об'єктів вікна (`.new`) та вкладок (`:AddTab`). Бібліотека резервує місце в пам'яті та створює контейнери в `CoreGui` або `PlayerGui`.
3. **Слухання подій (Event Loop):**
Після створення елементів (кнопки, слайдери), бібліотека переходить у режим очікування дій користувача. Вона використовує `InputBegan` та `MouseMoved` для відстеження взаємодії.
4. **Виконання Callback-функцій:**
При натисканні на елемент бібліотека миттєво запускає прив'язану до нього функцію. Весь код усередині `callback` виконується в окремому потоці, щоб не блокувати роботу самого інтерфейсу.

---

## 📂 Внутрішня ієрархія об'єктів

Кожен створений елемент отримує унікальний ідентифікатор. Це дозволяє системі:

* Коректно оновлювати стан (наприклад, змінювати текст кнопки).
* Зберігати значення в системі конфігурацій (якщо вона підтримується).
* Плавно анімувати лише ті частини UI, з якими взаємодіє користувач.

---

## ⚡️ Обробка вводу

**TerrorUi** автоматично обробляє пріоритети натискань:

* Якщо відкрито випадаючий список (**Dropdown**) або вибір кольору (**Color Picker**), кліки по кнопках під ними ігноруються.
* Меню автоматично перехоплює фокус введення, щоб запобігти випадковим діям у самій грі під час налаштування скрипта.

---

## 🧹 Завершення роботи

При виклику методу `GUI:Destroy()` бібліотека виконує повне очищення:

* Видаляє всі GUI-об'єкти з пам'яті.
* Відключає (Disconnect) усі події та цикли.
* Звільняє ресурси процесора, які використовувалися для анімацій.

---

[⬅ Назад до навігації](Navigation.md)
