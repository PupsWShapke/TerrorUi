# 🧠 Внутрішня логіка (Internal Logic)

Цей розділ пояснює архітектуру **TerrorUi** та те, як бібліотека обробляє дані, події та взаємодію між компонентами "під капотом".

---

## 🏗️ Модульна структура

Бібліотека побудована за принципом об'єктно-орієнтованого програмування (ООП). Кожен елемент інтерфейсу — це окремий об'єкт із власними властивостями та методами.

1. **Core (Ядро):** Головний об'єкт, який керує створенням вікон, обробкою тем та глобальними налаштуваннями.
2. **Container (Вкладки):** Логічні шари, які групують елементи. Вони керують видимістю своїх нащадків.
3. **Elements (Елементи):** Кнопки, слайдери, перемикачі. Кожен елемент має свій "слухач подій" (Event Listener).

---

## ⚡️ Система обробки подій (Signal System)

Коли ви взаємодієте з інтерфейсом, відбувається наступний ланцюжок дій:

* **Перехоплення вводу:** Бібліотека використовує `UserInputService`, щоб визначити, де знаходиться курсор і яка клавіша натиснута.
* **Валідація стану:** Перед виконанням дії система перевіряє, чи не перекритий елемент іншим вікном (наприклад, відкритим Dropdown).
* **Виклик Callback:** Якщо умови виконані, запускається ваша функція. Важливо: бібліотека використовує `task.spawn` або `coroutine.wrap`, щоб помилка у вашому коді не зупинила роботу всього інтерфейсу.

---

## 🎨 Система тем та рендерингу

Замість того, щоб жорстко прописувати кольори для кожного об'єкта, **TerrorUi** використовує централізовану таблицю констант.

* При зміні теми бібліотека обходить усі створені об'єкти та оновлює їх властивості (`BackgroundColor3`, `TextColor3` тощо).
* Анімації (Tweens) створюються динамічно в момент взаємодії, що дозволяє уникнути зайвого споживання пам'яті у стані спокою.

---

## 📂 Керування об'єктами в пам'яті

Для запобігання витоку пам'яті (Memory Leaks) бібліотека реалізує наступні механізми:

* **Auto-Cleanup:** При видаленні вкладки всі елементи всередині неї автоматично знищуються через `:Destroy()`.
* **Garbage Collection:** Бібліотека обнуляє посилання на об'єкти після їх видалення, дозволяючи вбудованому збирачу сміття Lua звільнити пам'ять.
* **Parenting:** Усі візуальні елементи створюються всередині одного `ScreenGui` в `CoreGui`, що робить їх незалежними від ігрового процесу.

---

## 🛡️ Захист та стабільність

* **Pcall wrapping:** Основні внутрішні функції обгорнуті в `pcall` (protected call), щоб захистити інтерфейс від крашів при некоректних вхідних даних (наприклад, якщо у `AddSlider` передати текст замість чисел).
* **Input Blocking:** Коли ви фокусуєтеся на текстовому полі (`Input`), бібліотека намагається заблокувати ігрові гарячі клавіші, щоб ваш персонаж не почав бігати під час друку.

---

[⬅ Назад до навігації](Navigation.md)
