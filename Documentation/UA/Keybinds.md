# ⌨️ Гарячі клавіші (Hotkeys)

Цей розділ описує механізми керування видимістю меню та використання клавіш для взаємодії з інтерфейсом.

---

## 🔑 Основна клавіша (Toggle Key)

За замовчуванням **TerrorUi** використовує спеціальну клавішу для швидкого приховування або відкриття головного вікна.

* **Клавіша за замовчуванням:** `RightControl` (Правий Ctrl) або `Insert`.
* **Функція:** Миттєво змінює стан видимості (`Visible`) головного фрейму, не перериваючи роботу скриптів.

---

## 🛠️ Вибір кольору (Color Picker)

Елемент вибору кольору має свої особливості взаємодії:

* **Натискання:** Відкриває палітру кольорів.
* **Перетягування:** Дозволяє плавно обирати відтінок у реальному часі.
* **Enter / Клік зовні:** Фіксує обраний колір та закриває вікно вибору.

---

## 📝 Текстові поля (Input)

При роботі з полями введення (`AddInput`):

* **Клавіша Enter:** Підтверджує введення тексту та запускає `callback`.
* **Клавіша Escape:** Скасовує введення та знімає фокус із текстового поля.

---

## 💡 Поради для зручності

1. **Конфлікти:** Якщо клавіша виклику меню збігається з ігровою дією, ви можете змінити її всередині вихідного коду або через спеціальний метод (якщо передбачено версією).
2. **Мобільні пристрої:** На телефонах замість гарячої клавіші використовується плаваюча кнопка на екрані.
3. **Фокус:** Під час друку в `Input`, керування персонажем у грі зазвичай автоматично блокується, щоб уникнути випадкових дій.

---

[⬅ Назад до навігації](Navigation.md)
