# 🚀 Поради з продуктивності

Для того, щоб ваш скрипт працював стабільно, а інтерфейс не спричиняв падіння FPS (кадрів на секунду), дотримуйтеся цих рекомендацій при роботі з **TerrorUi**.

---

## 🏎️ Оптимізація коду

1. **Уникайте важких циклів у Callback:**
Не запускайте нескінченні цикли `while true do` безпосередньо всередині функцій кнопок або перемикачів без затримок (`task.wait()`). Це може заморозити весь інтерфейс.
```lua
-- Погано:
Tab:AddToggle("Farm", false, function(state)
    while state do -- Це заблокує потік!
        print("farming")
    end
end)

-- Добре:
Tab:AddToggle("Farm", false, function(state)
    _G.Farm = state
    task.spawn(function()
        while _G.Farm do
            print("farming")
            task.wait(1)
        end
    end)
end)

```


2. **Використовуйте локальні змінні:**
Звернення до локальних змінних працює швидше, ніж до глобальних або постійний пошук об'єктів у `game.Workspace`.

---

## 📉 Графічна оптимізація

1. **Кількість елементів:**
Намагайтеся не створювати сотні кнопок в одній вкладці. Якщо функцій забагато, розподіліть їх між кількома вкладками. Це зменшить навантаження на рендеринг при відкритті меню.
2. **Оновлення списків (Dropdown):**
Використовуйте метод `:Refresh()` лише тоді, коли дані дійсно змінилися. Не оновлюйте список гравців щосекунди без потреби.
3. **Сповіщення (Notify):**
Не виводьте занадто багато сповіщень одночасно. Кожне нове сповіщення створює об'єкт з анімацією, що споживає ресурси відеокарти.

---

## 🧹 Керування пам'яттю

1. **Очищення при виході:**
Завжди використовуйте `GUI:Destroy()` у функціях деактивації скрипту. Це видаляє всі GUI-об'єкти, вимикає анімації та звільняє пам'ять, яку займав інтерфейс.
2. **Відключення подій:**
Якщо ви створюєте власні події (`game:GetService("RunService").RenderStepped`), обов'язково відключайте їх (`:Disconnect()`), коли користувач вимикає відповідний `Toggle`.

---

## 📱 Мобільні пристрої

Якщо ваш скрипт орієнтований на мобільних гравців:

* Вимикайте важкі візуальні ефекти (наприклад, складні градієнти), якщо це дозволяють налаштування.
* Перевіряйте роботу скрипту на низьких налаштуваннях графіки в самому Roblox.

---

[⬅ Назад до навігації](Navigation.md)
