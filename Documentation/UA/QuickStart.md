# 🚀 Швидкий старт

Цей розділ допоможе вам створити перше вікно за лічені секунди. Нижче наведено базовий код для ініціалізації інтерфейсу.

---

## ⚡️ Базовий приклад

Скопіюйте цей код у свій скрипт, щоб побачити роботу бібліотеки в дії:

```lua
local TerrorUi = loadstring(game:HttpGet("https://raw.githubusercontent.com/PupsWShapke/TerrorUi/refs/heads/main/Main.luau"))()

-- Ініціалізація вікна
local GUI = TerrorUi.new("My Script", "v1.2", "Midnight")

-- Створення вкладки
local Tab = GUI:AddTab("Main")

-- Додавання елементів
Tab:AddButton("Click Me", function()
    GUI:Notify("Notification", "Button has been pressed!", 3)
end)

Tab:AddToggle("Toggle Feature", false, function(state)
    print("Feature is: " .. tostring(state))
end)

```

---

## 🏗️ Кроки для створення

1. **Завантаження:** Виклик `loadstring` підключає ядро бібліотеки з репозиторію.
2. **Створення GUI:** Метод `.new` створює головний контейнер. Виберіть назву, версію та тему.
3. **Вкладки:** Використовуйте `:AddTab`, щоб розділити функції за категоріями.
4. **Елементи:** Наповнюйте вкладки кнопками, перемикачами та слайдерами для керування вашим кодом.

---

## 🔑 Гарячі клавіші за замовчуванням

* **Приховати/Показати меню:** Зазвичай використовується клавіша `RightControl` або `Insert`.
* **Закрити сповіщення:** Натисніть на саме сповіщення, щоб прибрати його раніше встановленого часу.

---

[⬅ Назад до навігації](Navigation.md)
