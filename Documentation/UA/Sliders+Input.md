# 🎚️ Слайдери та введення (Sliders & Inputs)

Ці елементи дозволяють користувачеві вводити точні дані: числові значення або текстові рядки.

---

## 📐 Слайдер (Slider)

Використовується для вибору числового значення в заданому діапазоні.

```lua
Tab:AddSlider(name, min, max, default, callback)

```

**Параметри:**

* `name` (string) — Назва параметра.
* `min` (number) — Мінімальне значення.
* `max` (number) — Максимальне значення.
* `default` (number) — Значення за замовчуванням.
* `callback` (function) — Функція, що отримує поточне число при зміні.

**Приклад:**

```lua
Tab:AddSlider("Швидкість", 16, 500, 16, function(value)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
end)

```

---

## ⌨️ Поле введення (Input)

Дозволяє користувачеві вводити довільний текст або числа вручну.

```lua
Tab:AddInput(name, placeholder, callback)

```

**Параметри:**

* `name` (string) — Назва поля.
* `placeholder` (string) — Текст-підказка, який відображається у порожньому полі.
* `callback` (function) — Функція, що отримує введений текст після натискання Enter.

**Приклад:**

```lua
Tab:AddInput("Повідомлення", "Введіть текст...", function(text)
    print("Користувач ввів: " .. text)
end)

```

---

## ⚙️ Особливості роботи

1. **Слайдери:** Підтримують як натискання в будь-яку точку смуги, так і плавне перетягування повзунка.
2. **Текстові поля:** Введення завершується лише після натискання клавіші **Enter**, що запобігає занадто частим викликам функції під час друку.
3. **Валідація:** Якщо слайдер потребує цілих чисел, ви можете використовувати `math.floor(value)` всередині вашої функції.

---

[⬅ Назад до навігації](Navigation.md)
