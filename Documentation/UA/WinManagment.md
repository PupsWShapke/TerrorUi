# 🪟 Керування вікнами (Window Management)

Цей розділ описує методи контролю головного вікна інтерфейсу, його стану та видимості.

---

## 🏗️ Створення вікна: `TerrorUi.new`

Метод ініціалізує основний фрейм та налаштовує його параметри.

```lua
local GUI = TerrorUi.new(title, version, theme)

```

**Параметри:**

* `title` (string) — Назва, що відображається у верхній панелі.
* `version` (string) — Текст версії поруч із назвою.
* `theme` (string) — Початкова тема (`"Default"` або `"Midnight"`).

---

## 👁️ Видимість та перемикання

Ви можете програмно керувати тим, чи бачить користувач інтерфейс у даний момент.

### Метод `Toggle`

Змінює стан видимості на протилежний (якщо відкрито — закриває, і навпаки).

```lua
GUI:Toggle()

```

### Налаштування клавіші виклику

Бібліотека автоматично реєструє клавішу для приховування меню (зазвичай **RightControl**). Це дозволяє гравцеві швидко прибрати UI з екрана під час гри.

---

## 🔔 Сповіщення: `Notify`

Вікно може виводити швидкі повідомлення у кутку екрана для інформування користувача.

```lua
GUI:Notify(title, text, duration)

```

**Параметри:**

* `title` (string) — Заголовок сповіщення.
* `text` (string) — Основний текст повідомлення.
* `duration` (number) — Час у секундах, через який сповіщення зникне.

---

## 🧹 Закриття та очищення: `Destroy`

Якщо потрібно повністю видалити інтерфейс та зупинити всі його процеси (наприклад, при вивантаженні скрипта).

```lua
GUI:Destroy()

```

*Цей метод видаляє всі об'єкти з CoreGui та розриває внутрішні зв'язки для звільнення пам'яті.*

---

## 📌 Особливості поведінки

1. **Drag & Drop:** Головне вікно можна перетягувати за верхню панель (Header).
2. **Auto-Sizing:** Розмір вікна автоматично адаптується під кількість вкладок у боковому меню.
3. **ZIndex:** Вікно завжди намагається бути поверх стандартних елементів гри, щоб уникнути перекриття.

---

[⬅ Назад до навігації](Navigation.md)
